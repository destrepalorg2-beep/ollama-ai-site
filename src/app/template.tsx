/**
 * Route transitions are off — the page-turn animation was removed on request.
 *
 * The file stays as a pass-through rather than being deleted so nothing else
 * has to change; it can be deleted outright if you want it gone for good.
 */
export default function Template({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
